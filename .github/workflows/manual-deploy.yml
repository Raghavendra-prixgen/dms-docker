# name: Manual Deploy

# on:
#   workflow_dispatch:

# jobs:
#   deploy:
#     runs-on: self-hosted

#     steps:
#       - run: |
#           cd /opt/odoo
#           git pull origin main
#           docker compose up -d